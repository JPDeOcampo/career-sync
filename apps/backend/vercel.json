{
  "version": 2,
  "buildCommand": "pnpm --filter @career-sync/shared build && pnpm --filter backend build",
  "builds": [
    {
      "src": "dist/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/server.js"
    }
  ]
}
